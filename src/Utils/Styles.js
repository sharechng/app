import { StyleSheet } from "react-native";

const ToastStyling = StyleSheet.create({
  textStyle: {
    fontFamily: "Montserrat-Medium",
    fontSize: height / 55,
  },
  style: {
    width: width * 0.92,
    borderRadius: 5,
    margin: 15,
    alignItems: "center",
  },
});

export default ToastStyling;
